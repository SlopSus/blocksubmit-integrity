import { useState } from "react";import { useNavigate } from "@tanstack/react-router";import { ArrowRight,FileCheck2,LockKeyhole,ShieldCheck } from "lucide-react";import { useAuth } from "@/context/auth-context";import { Logo } from "@/components/blocksubmit/shared";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";
export function LoginPage(){const {login}=useAuth();const nav=useNavigate();const [busy,setBusy]=useState(false);const [error,setError]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const form=new FormData(e.currentTarget);try{const u=await login({email:String(form.get("email")),password:String(form.get("password"))});nav({to:u.role==="professor"?"/professor":"/student"})}catch(e){setError(e instanceof Error?e.message:"Unable to sign in.")}finally{setBusy(false)}}return <main className="grid min-h-screen lg:grid-cols-[1.1fr_.9fr]"><section className="relative hidden overflow-hidden border-r bg-surface p-12 lg:flex lg:flex-col"><Logo/><div className="my-auto max-w-xl"><p className="text-xs font-bold text-primary">DOCUMENT · INTEGRITY · PROOF</p><h1 className="mt-5 text-5xl font-semibold leading-[1.08]">Academic work,<br/>anchored in trust.</h1><p className="mt-6 max-w-lg text-base leading-7 text-muted-foreground">A secure submission workspace where every document fingerprint can be independently verified against its trusted record.</p><div className="mt-10 grid grid-cols-3 gap-3"><Visual icon={FileCheck2} label="Document"/><Visual icon={ShieldCheck} label="Integrity"/><Visual icon={LockKeyhole} label="Proof"/></div></div><p className="text-xs text-muted-foreground">BlockSubmit · University integrity infrastructure</p></section><section className="flex items-center justify-center p-5 sm:p-10"><div className="w-full max-w-md"><div className="mb-10 lg:hidden"><Logo/></div><p className="text-xs font-bold text-primary">SECURE ACCESS</p><h2 className="mt-3 text-3xl font-semibold">Welcome back</h2><p className="mt-2 text-sm text-muted-foreground">Sign in with your university account.</p><form onSubmit={submit} className="mt-8 space-y-5"><div><Label htmlFor="email">Email</Label><Input id="email" name="email" type="email" placeholder="name@blocksubmit.edu" className="mt-2 h-11" required/></div><div><Label htmlFor="password">Password</Label><Input id="password" name="password" type="password" placeholder="Enter your password" className="mt-2 h-11" minLength={4} required/></div>{error&&<p className="rounded-md border border-destructive/25 bg-destructive/10 p-3 text-sm text-destructive">{error}</p>}<Button className="h-11 w-full" disabled={busy}>{busy?"Signing in…":<>Sign in<ArrowRight/></>}</Button></form><div className="mt-7 rounded-md border bg-surface p-4 text-xs text-muted-foreground"><p className="font-semibold text-foreground">Demo access</p><p className="mt-2">Professor: professor@blocksubmit.edu</p><p>Student: student@blocksubmit.edu</p><p className="mt-1">Use any password with 4 or more characters.</p></div></div></section></main>}
function Visual({icon:Icon,label}:{icon:any;label:string}){return <div className="rounded-md border bg-background/30 p-4"><Icon className="size-5 text-primary"/><p className="mt-3 text-sm font-semibold">{label}</p></div>}
