import { useEffect,useState } from "react";import { Activity as ActivityIcon,KeyRound,MonitorCog,Shield,UserRound } from "lucide-react";import { activityService } from "@/services";import type { Activity } from "@/types/domain";import { ActivityList } from "./dashboards";import { PageHeading } from "@/components/blocksubmit/shared";import { useAuth } from "@/context/auth-context";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";
export function ActivityPage(){const [items,setItems]=useState<Activity[]>([]);useEffect(()=>{activityService.list().then(setItems)},[]);return <div className="space-y-7"><PageHeading eyebrow="AUDIT TRAIL" title="Activity" description="A chronological view of assignment, submission, and integrity events."/><div className="max-w-3xl"><ActivityList items={items}/></div></div>}
export function SettingsPage(){const {user}=useAuth();if(!user)return null;return <div className="space-y-7"><PageHeading eyebrow="ACCOUNT CONTROL" title="Settings" description="Manage your profile and application preferences."/><div className="grid gap-5 lg:grid-cols-[220px_1fr]"><nav className="space-y-1"><SettingNav icon={UserRound} label="Profile" active/><SettingNav icon={KeyRound} label="Security"/><SettingNav icon={MonitorCog} label="Application"/></nav><section className="panel max-w-2xl rounded-lg p-6"><div className="flex items-center gap-3 border-b pb-5"><span className="grid size-11 place-items-center rounded-md bg-primary/10 font-bold text-primary">{user.initials}</span><div><h2 className="font-semibold">Profile information</h2><p className="text-sm text-muted-foreground">Your university identity</p></div></div><div className="mt-6 grid gap-5 sm:grid-cols-2"><div><Label>Name</Label><Input className="mt-2" value={user.name} readOnly/></div><div><Label>Email</Label><Input className="mt-2" value={user.email} readOnly/></div><div><Label>Department</Label><Input className="mt-2" value={user.department} readOnly/></div><div><Label>Role</Label><Input className="mt-2 capitalize" value={user.role} readOnly/></div></div><Button className="mt-6" disabled>Managed by university</Button></section></div></div>}
function SettingNav({icon:Icon,label,active=false}:{icon:any;label:string;active?:boolean}){return <button className={active?"flex w-full items-center gap-3 rounded-md bg-primary/10 px-3 py-2.5 text-sm font-medium text-primary":"flex w-full items-center gap-3 rounded-md px-3 py-2.5 text-sm font-medium text-muted-foreground"}><Icon className="size-4"/>{label}</button>}
